# feature/b の作業内容

`feature/b` ブランチでは、`conflict_note.md` の次の行を変更します。

変更前:

```text
結論: このデータから分かることをここに書く。
```

変更後:

```text
結論: 条件Bでは測定値にばらつきがある。
```

commit メッセージ:

```bash
git commit -m "条件Bの結論を追加"
```
